#include "GreensFct.hpp"

/*class WeissFctWK: public GreensFct
{
    public:
        EIGEN_MAKE_ALIGNED_OPERATOR_NEW
        WeissFctWK(const RealT zeroShift): GreensFct(RealT beta), zeroShift(
        {
        }


    private:
        RealT zeroShift;

        /*! compute auxiliary ising spin shift to G0(0).
         *
         *  @param  [in]  s     ising spin (-1 or 1)
         *  @param  [in]  sigma spin (-1 or 1)
         */
        inline RealT alpha(int sigma, int s)
        {
            return 0.5 + sigma*s*(0.5 + zeroShift);
        }
        VectorT auxSpins;
};*/
